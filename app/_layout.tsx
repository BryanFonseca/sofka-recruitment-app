import { Stack } from "expo-router";

function Layout() {
    return (
        <Stack
            screenOptions={{
                contentStyle: {
                    padding: "5%",
                },
            }}
        />
    );
}

export default Layout;
